<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee 
         https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>Sistema de Inventario - Ferretería Backend</display-name>
    <description>
        Backend del sistema de control de inventario para ferretería con arquitectura N-Capas
        y servicios web SOAP para gestión de artículos.
    </description>

    <!-- Configuración del servlet para servicios SOAP -->
    <servlet>
        <servlet-name>InventarioWebService</servlet-name>
        <servlet-class>com.sun.xml.ws.transport.http.servlet.WSServlet</servlet-class>
        <load-on-startup>1</load-on-startup>
    </servlet>
    
    <servlet-mapping>
        <servlet-name>InventarioWebService</servlet-name>
        <url-pattern>/InventarioService</url-pattern>
    </servlet-mapping>

    <!-- Filtro de seguridad para servicios SOAP -->
    <filter>
        <filter-name>AuthenticationFilter</filter-name>
        <filter-class>com.ferreteria.inventario.filter.AuthenticationFilter</filter-class>
    </filter>
    
    <filter-mapping>
        <filter-name>AuthenticationFilter</filter-name>
        <url-pattern>/InventarioService/*</url-pattern>
        <dispatcher>REQUEST</dispatcher>
    </filter-mapping>

    <!-- Configuración de seguridad básica -->
    <security-constraint>
        <web-resource-collection>
            <web-resource-name>Servicios SOAP Protegidos</web-resource-name>
            <url-pattern>/InventarioService/*</url-pattern>
        </web-resource-collection>
        <auth-constraint>
            <role-name>ADMIN</role-name>
            <role-name>OPERADOR</role-name>
            <role-name>CONSULTA</role-name>
        </auth-constraint>
    </security-constraint>

    <login-config>
        <auth-method>BASIC</auth-method>
        <realm-name>Inventario Ferretería</realm-name>
    </login-config>

    <security-role>
        <role-name>ADMIN</role-name>
    </security-role>
    <security-role>
        <role-name>OPERADOR</role-name>
    </security-role>
    <security-role>
        <role-name>CONSULTA</role-name>
    </security-role>

    <!-- Configuración de sesión -->
    <session-config>
        <session-timeout>30</session-timeout>
        <cookie-config>
            <http-only>true</http-only>
            <secure>false</secure>
        </cookie-config>
    </session-config>

    <!-- Configuración de tipos MIME -->
    <mime-mapping>
        <extension>wsdl</extension>
        <mime-type>text/xml</mime-type>
    </mime-mapping>
    
    <mime-mapping>
        <extension>xsd</extension>
        <mime-type>text/xml</mime-type>
    </mime-mapping>

</web-app>
